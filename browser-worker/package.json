{
  "name": "browser-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Worker isolado que busca páginas para a Central de Monitoramento. Deployável separadamente da API.",
  "engines": { "node": ">=22.0.0" },
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test test/*.test.mjs"
  }
}
