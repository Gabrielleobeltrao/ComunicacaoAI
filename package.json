{
  "name": "comunicacaoai",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend",
    "backend",
    "runner",
    "browser-worker"
  ],
  "scripts": {
    "dev": "concurrently -n frontend,backend -c blue,green \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "build": "npm run build -w frontend && npm run build -w backend",
    "dev:api": "concurrently -n frontend,backend -c blue,green \"npm run dev -w frontend\" \"npm run dev:api -w backend\"",
    "smoke": "npm run build && node scripts/mvp-smoke.mjs",
    "secret-scan": "node scripts/secret-scan.mjs",
    "test:runner": "npm test -w runner",
    "test:browser-worker": "npm test -w browser-worker"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
